services:
  - type: web
    name: corax-backend

    env: node

    plan: free

    buildCommand: npm install

    startCommand: node server.js

    autoDeploy: true

    envVars:
      - key: PORT
        value: 10000

      - key: JWT_SECRET
        sync: false

      - key: MONGO_URI
        sync: false
